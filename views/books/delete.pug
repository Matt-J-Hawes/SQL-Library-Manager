extends ../layout

block content
  div.subheader
   h2= title
  p 
    | Are you sure you want to delete 
    em= book.title
    | ?
  form(action="/books/" + book.id + "/delete", method="post")
    a(href="/books/" + book.id) No! Go back.
    | 
    input(type="submit", value = "Yes, I'm sure")